<h1>Here's task number <%= @task.id %></h1>
<div> <%= @task.title %></div>
<div> <%= @task.description %></div>
<div> <%= @task.assign_to %></div>
<div>
  <i><%= link_to "", task_path,
  method: :delete,
  data: { confirm: "Are you sure?"}, class: "fas fa-trash right-top-icons" %>
</i>
</div>

<div>
  <%= link_to "Delete", task_path,
  method: :delete,
  data: { confirm: "Are you sure?"} %>
</div>
<div> <%= link_to "New task", new_task_path %></div>
<div> <%= link_to "All tasks", tasks_path %></div>
